# Gemmary — Agent Rules

## Userscript conventions

- Add a Tampermonkey install badge to README.md when creating or updating a userscript.
- Run `npm run check` (`biome check` + `markdownlint-cli2`)
  before finishing any code change.
